var React = require('react');

var Footer = React.createClass({
    render: function(){
        return(
            <div>
                this is the footer
            </div>
        )
    }
});

module.exports = Footer;
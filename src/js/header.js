var React = require('react');

var Header = React.createClass({
    render: function(){
        return(
            <div>
                this is the header
            </div>
        )
    }
});

module.exports = Header;